SELECT name, routes, color FROM nyc_subway_stations ORDER BY routes ASC;

SELECT name, routes, color FROM nyc_subway_stations ORDER BY routes ASC LIMIT 10;

SELECT name, routes, color FROM nyc_subway_stations ORDER BY name DESC;